# Copy this file to .env.local and fill in real values.
# The site degrades gracefully if these are missing (contact form/presence
# indicator just show as unconfigured instead of crashing).

# Resend — used by the contact form (src/app/api/contact/route.ts)
RESEND_API_KEY=
CONTACT_TO_EMAIL=jerwinglen0427@gmail.com

# Supabase — used by the presence indicator (src/lib/supabase.ts)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
